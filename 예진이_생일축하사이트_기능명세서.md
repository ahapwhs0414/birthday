# 예진이 생일 축하 인터랙티브 사이트 기능 명세서

문서 버전: 0.2.0  
문서 목적: 예진이 전용 최초 프로토타입의 구현 기준 정의  
대상 사용자: 예진이와 예진이의 지인  
우선순위: 모바일 사용성, 따뜻한 감정 전달, 공유와 기록의 안정성

## 1. 제품 개요

이 사이트는 예진이의 지인에게 링크로 공유된다. 지인은 짧은 생일 축하 스토리를 읽고, 자신의 이름과 관계를 입력한 뒤, 예진이를 돕는 두 가지 미니게임을 수행한다. 마지막으로 축하 메시지를 남기면 이름·관계·게임 결과·축하 메시지가 기록된다.

예진이는 별도의 결과 화면에서 여러 사람이 남긴 기록을 모아 볼 수 있다. 최초 버전은 예진이 한 명만을 위한 전용 사이트로 제작하지만, 스토리·게임·수신자 정보를 콘텐츠 설정으로 분리해 추후 다른 사람이 자신의 지인을 위한 축하 사이트를 생성할 수 있도록 설계한다.

### 1.1 최초 프로토타입 고정 정보

| 항목 | 값 |
|---|---|
| 수신자 이름 | 예진 |
| 생일 | 2005년 9월 20일 |
| 생일 당시 나이 | 22번째 생일 |
| 최초 콘텐츠 | 예진이 전용으로 고정 |
| 주요 접속 기기 | 스마트폰 세로 화면 |
| 참여 방식 | 링크를 받은 지인이 회원가입 없이 참여 |

나이는 실제 서비스 시점에 따라 날짜 계산 방식으로 바꿀 수 있지만, 최초 프로토타입의 문구와 결과 화면에서는 “예진이의 22번째 생일”을 사용한다.

### 1.2 최초 프로토타입 기술·운영 환경

최초 프로토타입은 무료 요금제에서 운영 가능한 다음 조합을 기준으로 구현한다.

| 영역 | 결정 | 적용 이유 |
|---|---|---|
| 언어 | TypeScript | 화면 상태·게임 답안·저장 데이터의 타입 오류를 줄이고 Codex가 일관되게 수정하기 쉽다. |
| 웹 프레임워크 | Next.js App Router + React | 모바일 화면과 인터랙션을 한 프로젝트에서 구현하고, Vercel 배포 및 서버 기능을 함께 사용하기 쉽다. |
| 스타일 | Tailwind CSS + 일반 CSS | 반응형 화면을 빠르게 구성하되, 생일 사이트의 세밀한 애니메이션과 캐릭터 배치는 일반 CSS로 보완한다. |
| 패키지 실행 | Node.js LTS + npm | 사용자가 기존 프로젝트에서 사용해 온 방식과 맞추고 초기 설정을 단순하게 유지한다. |
| 배포 | Vercel Hobby | Next.js에 최적화된 무료 배포를 사용하고 GitHub 저장소의 `main` 브랜치와 연결한다. 첫 버전은 Vercel이 제공하는 `vercel.app` 주소를 사용한다. |
| 데이터베이스 | Supabase Free PostgreSQL | 참여 기록과 수신자 데이터를 저장하고, 추후 다중 캠페인으로 확장할 수 있는 관계형 데이터베이스를 사용한다. |
| 결과 페이지 인증 | Supabase Auth | 참여자는 로그인하지 않고, 예진이용 기록 화면만 소유자 계정으로 보호한다. |
| 데이터 접근 | `@supabase/ssr` + `@supabase/supabase-js` | Next.js 서버·브라우저 환경에 맞는 Supabase 클라이언트를 사용한다. |
| 이미지 | 최초에는 저장소 `public/` 폴더 | 이미지 수가 적은 최초 버전에서 외부 스토리지 의존성을 줄인다. 추후 콘텐츠 편집기에서 Supabase Storage를 사용한다. |
| ORM | 새 프로젝트에서는 사용하지 않음 | 단순한 CRUD와 Vercel 서버리스 환경에는 Supabase SDK가 충분하며, Prisma 7의 연결·풀링 설정을 추가하지 않는다. |

이 결정은 “영구적으로 무료”를 보장한다는 뜻이 아니다. 무료 요금제의 한도와 정책은 변경될 수 있으므로 배포 시점에 공식 요금제와 제한을 다시 확인한다. Supabase Free 프로젝트는 일정 기간 미사용 시 일시 중지될 수 있고 자동 백업이 제공되지 않으므로, 생일 행사 전후로 데이터 내보내기를 수행한다.

공식 참고 문서: [Vercel 요금제](https://vercel.com/pricing), [Vercel 제한](https://vercel.com/docs/limits), [Supabase 요금제](https://supabase.com/pricing), [Supabase Row Level Security](https://supabase.com/docs/guides/database/postgres/row-level-security), [Next.js App Router](https://nextjs.org/docs/app)

## 2. 목표와 비목표

### 2.1 목표

1. 링크를 받은 사람이 설명 없이도 자연스럽게 스토리와 게임을 끝까지 진행할 수 있어야 한다.
2. 예진이가 여러 사람의 이름과 메시지를 한 화면에서 확인할 수 있어야 한다.
3. 퀴즈와 실습 결과가 마지막 캐릭터와 기록에 반영되어야 한다.
4. 카카오톡 등 메신저에서 링크를 공유했을 때 제목·설명·대표 이미지가 적절히 표시되어야 한다.
5. 스토리 문구와 게임 콘텐츠를 화면 흐름 코드와 분리해 수정하기 쉬워야 한다.
6. 이후 여러 수신자를 지원할 수 있도록 수신자·콘텐츠·기록의 구조를 확장 가능하게 잡아야 한다.

### 2.2 최초 버전의 비목표

- 일반 사용자가 사이트 안에서 수신자와 스토리를 직접 편집하는 관리자 화면
- 참여자 대상 로그인·회원가입 기반의 사용자 계정 체계
- 결제, 광고, 공개 검색, 소셜 로그인
- 복잡한 랭킹이나 경쟁 기능
- 참여자의 실명 인증
- 정교한 캐릭터 커스터마이징 편집기

최초 버전은 콘텐츠 설정 파일이나 코드의 한정된 영역을 수정하는 방식으로 운영한다. 다만 화면 흐름을 다시 작성하지 않고 문구·이미지·선택지·정답·결과 매핑을 바꿀 수 있어야 한다.

## 3. 사용자 유형과 권한

### 3.1 참여자

링크를 받은 예진이의 지인이다.

- 이름 또는 별명을 입력할 수 있다.
- 관계를 선택하거나 직접 입력할 수 있다.
- 스토리를 읽고 게임을 진행할 수 있다.
- 축하 메시지를 남길 수 있다.
- 제출 후 자신의 결과 요약을 볼 수 있다.
- 다른 참여자의 기록 전체를 기본적으로 볼 수 없다.

### 3.2 예진이

축하를 받는 사람이다.

- 별도 결과 페이지에서 누적된 기록을 볼 수 있다.
- 기록을 최신순·이름순으로 정렬할 수 있으면 좋다.
- 각 참여자의 이름, 관계, 축하 메시지, 게임 결과, 제출 시각을 확인할 수 있다.
- 최초 프로토타입에서는 예진이용 결과 페이지를 Supabase Auth 소유자 계정으로 보호한다. 숨겨진 URL은 보조적인 경로 구분일 뿐 인증을 대신하지 않는다.

### 3.3 운영자/개발자

최초 버전의 콘텐츠를 수정하는 사람이다.

- 수신자 정보와 스토리 순서를 수정한다.
- 게임 문구·선택지·정답·이미지·결과 표현을 수정한다.
- 데이터 저장 오류와 배포 상태를 확인한다.

## 4. 전체 사용자 여정

```text
공유 링크 접속
  → 오프닝 스토리 4장
  → 참여자 이름·관계 입력
  → 시험 안내
  → 5초 생일 퀴즈
  → 시험 결과
  → 실습 안내
  → 준비물 선택 3라운드
  → 실습 결과
  → 축하 메시지 입력
  → 최종 캐릭터·전체 결과 표시
  → 기록 저장
```

페이지 새로고침이나 뒤로 가기에도 가능한 범위에서 입력 내용과 진행 상태를 잃지 않게 한다. 단, 최종 제출 전의 임시 상태는 브라우저 로컬 저장소에 보관할 수 있고 서버 기록으로 저장하지 않는다.

## 5. 화면 및 기능 상세

### 5.1 공통 UI 원칙

- 모바일 세로 화면을 우선한다.
- 한 화면에 핵심 행동 하나만 두고, 버튼은 엄지손가락으로 누르기 쉬운 크기로 만든다.
- 스와이프와 “다음” 버튼을 모두 지원한다. 스와이프만 지원해 사용자가 진행 방법을 놓치지 않게 한다.
- 상단에는 현재 진행 위치를 알 수 있는 점 표시 또는 진행률을 제공한다.
- 모든 화면에 과도한 장식과 긴 문단을 피하고, 짧은 문장·큰 글씨·충분한 여백을 사용한다.
- 애니메이션은 감정을 돕는 수준으로 사용하며, `prefers-reduced-motion` 사용자를 위해 줄일 수 있어야 한다.
- 이미지 로딩 중에는 빈 화면 대신 플레이스홀더를 보여준다.
- 버튼의 비활성·로딩·오류 상태를 명확히 보여준다.

### 5.2 오프닝 스토리

스토리는 카드 또는 풀스크린 슬라이드 형태다. 이미지와 텍스트가 화면 중앙에 배치된다. 마지막 장에서 다음 단계로 이동하는 행동을 명확히 표시한다.

| 순서 | 이미지/연출 | 문구 | 완료 조건 |
|---:|---|---|---|
| 1 | 큰 초 2개와 작은 초 2개가 꽂힌 케이크 | “예진이의 22번째 생일을 축하해주세요!” | 다음 스와이프 또는 버튼 |
| 2 | 울고 있는 여자 캐릭터 | “하지만 예진이는 너무 슬퍼요” | 다음 스와이프 또는 버튼 |
| 3 | 여자 캐릭터 위에 무거운 짐들이 올라간 이모티콘 | “왜냐하면 생일인데도 예진이는 할 일이 너무 많기 때문이에요” | 다음 스와이프 또는 버튼 |
| 4 | 도움을 요청하는 이모티콘 | “예진이가 생일 동안 쉴 수 있도록 여러분의 도움이 필요해요!” | “도와주기” 버튼 |

문구는 콘텐츠 설정에서 수정 가능해야 한다. 이미지가 준비되지 않은 초기 개발 단계에서는 동일한 크기의 일러스트 플레이스홀더를 사용하되, 실제 배포 전에는 최종 이미지로 교체한다.

권장 추가 기능:

- 첫 화면에 배경 음악을 자동 재생하지 않는다. 사용자가 원할 때 켤 수 있는 음악 버튼을 둔다.
- 스토리 재생 중에는 화면을 탭해 다음으로 이동할 수 있도록 하되, 실수로 넘긴 경우 이전 버튼을 제공한다.
- 공유 링크로 처음 들어온 경우 “소리를 켜면 더 재미있어요” 같은 짧은 안내를 한 번만 보여줄 수 있다.

### 5.3 참여자 정보 입력

스토리 마지막 이후 다음 문구를 표시한다.

> 예진이를 도와줄 당신의 이름은 무엇인가요?

보조 문구:

> 실명이 아니어도 괜찮아요. 친구들이 알아볼 수 있는 별명으로 남겨도 좋아요.

입력 항목:

| 항목 | 필수 | 동작 |
|---|---:|---|
| 이름 또는 별명 | 예 | 공백만 입력할 수 없음, 최대 30자 권장 |
| 관계 | 아니오 | 친구, 가족, 연인, 직장/학교 지인, 직접 입력, 비공개 |
| 직접 입력 관계 | 관계에서 “직접 입력”을 선택했을 때만 | 최대 20자 권장 |

“다음” 버튼은 이름이 유효할 때만 활성화한다. 이름은 실명을 요구하지 않는다. 이름과 별명을 동시에 입력하는 기능은 최초 버전에서 필수로 만들지 않고 데이터 모델의 확장 필드로 예약한다. 추후 예진이가 별명만 보고 실명을 맞히는 기능을 추가할 수 있도록 `displayName`, `realName`, `nameVisibility`를 분리할 수 있는 구조를 고려한다.

### 5.4 시험 안내 화면

울고 있는 여자 캐릭터와 다음 문구를 표시한다.

> 시험 기간에도 공부해야 하는 예진이를 위해서 몰래 답을 알려주세요!

“시험 시작” 버튼을 누르면 문제 화면으로 이동한다.

### 5.5 시험: 생일 맞히기 퀴즈

#### 문제

> 예진이의 생일은 언제일까요?

선택지는 다음과 같다.

1. 2006년 8월 20일
2. 2005년 8월 20일
3. 2005년 8월 21일
4. 2005년 9월 20일 — 정답

#### 진행 규칙

- 제한 시간은 5초다.
- 문제 화면 진입과 동시에 카운트다운을 시작한다.
- 선택지를 누르면 즉시 정답 여부를 확정한다.
- 시간이 0초가 되면 미응답 오답으로 처리한다.
- 제출 중에는 중복 클릭을 막는다.
- 새로고침 후에도 이미 확정된 답을 다시 바꾸지 못하도록 임시 상태를 보존한다.
- 시험 점수는 정답이면 100점, 오답 또는 시간 초과면 0점이다.

#### 시험 결과

성공:

- 시험지에 100점이 표시된 연출
- 기뻐하는 예진이 캐릭터 또는 적절한 이모티콘
- 문구: “예진이가 시험에 100점을 맞았어요!”
- “실습하러 가기” 버튼

실패:

- 더 크게 우는 예진이 캐릭터 또는 적절한 이모티콘
- 문구: “예진이의 생일은 2005년 9월 20일이에요. 다음부터는 꼭 기억해 주세요!”
- 시험지에 0점이 표시된 연출
- “그래도 실습하러 가기” 버튼

### 5.6 실습 안내 화면

파자마를 입고 피곤해하며 하품하는 여자 캐릭터를 보여준다.

문구:

> 내일 출근해야 하는 예진이는 준비하는 게 제일 힘들어요. 예진이의 짐을 대신 챙겨주세요!

“실습 시작” 버튼을 누르면 선택 게임을 시작한다.

### 5.7 실습: 준비물 선택 게임

화면 배치:

- 위쪽 중앙: 예진이 캐릭터와 현재 상태 얼굴
- 아래쪽: 선택지 2개를 큰 카드 또는 버튼으로 표시
- 선택 후: 선택 결과를 짧게 피드백하고 다음 라운드로 이동

공통 규칙:

- 총 3라운드다.
- 각 라운드는 두 선택지 중 하나를 고르는 방식이다.
- 정답을 고르면 웃는 얼굴, 오답을 고르면 슬픈 얼굴을 표시한다.
- 선택 결과에 따라 예진이의 옷 또는 준비물 일러스트를 누적한다.
- 정답 여부만 알려주고, 사용자를 조롱하거나 실패를 과도하게 강조하지 않는다.
- 마지막에 총 정답 개수를 표시한다.

| 라운드 | 선택지 A | 선택지 B | 정답 | 결과 반영 |
|---:|---|---|---|---|
| 1 | 간호사 옷 | 소방관 옷 | 간호사 옷 | 캐릭터의 직업 의상 영역 |
| 2 | 제로 슈거 사탕 | 슈거 사탕 | 슈거 사탕 | 간식/준비물 영역 |
| 3 | 크록스 | 간호화 | 간호화 | 신발 영역 |

마지막 결과 예시:

> 실습 결과: 3개 중 2개를 맞혔어요!

최종 캐릭터에는 선택된 항목을 반영한다. 정답 항목만 반영할지, 사용자가 고른 항목을 모두 반영할지는 구현 전 결정할 수 있지만, 사용자의 선택이 결과에 보였다는 느낌을 주기 위해 다음을 권장한다.

- 캐릭터에는 사용자가 선택한 의상·간식·신발을 반영한다.
- 정답 항목에는 작은 체크 표시, 오답 항목에는 귀여운 경고 표시를 추가한다.
- 최종적으로 예진이가 준비를 마친 모습으로 보이게 한다.

### 5.8 축하 메시지 입력

실습 결과 이후 다음 문구를 표시한다.

> 여러분 덕분에 예진이의 생일이 행복해졌어요!

이름을 입력한 경우:

> 마지막으로 [입력받은 이름]의 축하 한마디가 예진이의 생일을 더 행복하게 만들 거예요!

이름은 화면에 HTML로 직접 삽입하지 말고 안전하게 텍스트로 렌더링한다.

입력 항목:

- 축하 메시지: 필수
- 최대 길이: 300자 권장
- 줄바꿈 허용
- 빈 문자열·공백만 입력 불가
- 제출 전 입력 내용을 미리 확인할 수 있으면 좋다.

메시지 예시 placeholder:

> 예진아, 22번째 생일 정말 축하해! 오늘은 꼭 행복하게 쉬어야 해 🎂

### 5.9 최종 결과 화면

제출이 완료되면 다음 요소를 한 화면에 보여준다.

1. 입력한 이름
2. 시험 결과를 나타내는 100점 또는 0점 시험지
3. 실습 선택 결과가 반영된 여자 캐릭터
4. 실습 정답 개수
5. 사용자가 남긴 축하 메시지
6. 다음 문구

> [이름] 덕분에 예진이의 22번째 생일이 더욱더 행복해졌어요!

버튼:

- “내 기록 다시 보기”
- “친구에게 축하 참여 링크 보내기”
- “예진이에게 남긴 메시지 공유하기”는 최초 버전에서 선택 기능으로 둘 수 있다.

최종 제출은 한 번만 저장한다. 동일 사용자가 새로고침하거나 제출 버튼을 여러 번 눌러도 중복 기록이 생기지 않도록 클라이언트 요청 키 또는 서버 중복 방지 로직을 둔다.

## 6. 예진이용 기록 모아보기

예진이가 지인들의 축하를 확인할 수 있는 별도 화면을 제공한다.

### 6.1 기본 표시

- 전체 참여자 수
- 최근 참여자 카드 목록
- 카드별 이름/별명
- 관계(비공개이면 표시하지 않음)
- 축하 메시지
- 시험 점수
- 실습 점수
- 제출 날짜와 시간

### 6.2 필터와 정렬

최초 버전에서 다음 중 최소한 최신순 정렬을 제공한다.

- 최신순
- 이름순
- 시험 만점자만 보기
- 실습 점수별 보기

### 6.3 접근 보호

축하 기록은 개인적인 메시지를 포함할 수 있으므로, 누구나 접근 가능한 공개 링크와 분리한다. 최초 버전에서는 Supabase Auth로 예진이 소유자 계정을 인증하고, 서버와 RLS에서 소유자 권한을 확인한다. 긴 비공개 경로나 결과 페이지 비밀번호는 보조 수단으로만 고려하며 인증을 대체하지 않는다.

참여자는 기본적으로 전체 기록 페이지에 접근하지 않는다. 기록 API가 있다면 수신자용 권한 검사를 서버에서 수행한다.

### 6.4 추가로 권장하는 감정 기능

구현 난이도와 개인정보를 고려해 다음 순서로 검토한다.

1. 메시지 카드에 하트 반응을 남기는 기능
2. 예진이가 메시지를 읽었는지 표시하는 기능
3. 전체 메시지를 자동으로 넘겨보는 “축하 메시지 상영회” 모드
4. “가장 많이 도와준 사람” 같은 경쟁 표현 대신, 랜덤으로 한 명의 메시지를 크게 보여주는 기능

점수 경쟁이나 순위는 생일 축하의 분위기를 해칠 수 있으므로 기본 기능으로 넣지 않는다.

## 7. 공유와 검색 미리보기

공유 링크가 카카오톡·문자·SNS에서 의미 있는 카드로 보이도록 메타데이터를 제공한다.

### 7.1 기본 메타데이터

- 페이지 제목: “예진이의 22번째 생일을 함께 축하해주세요 🎂”
- 설명: “예진이를 도와주고, 간단한 게임을 한 뒤 축하 메시지를 남겨주세요!”
- 대표 이미지: 케이크와 예진이 캐릭터가 함께 있는 세로형 또는 정방형 이미지
- `og:title`, `og:description`, `og:image`, `og:url`
- `twitter:card=summary_large_image`
- 대표 이미지의 실제 절대 URL과 적절한 크기

### 7.2 공유 동작

- Web Share API가 지원되면 기기 기본 공유를 우선 사용한다.
- 지원하지 않으면 링크 복사 버튼을 제공한다.
- 링크 복사 완료 상태를 토스트로 알려준다.
- 카카오톡 전용 SDK 연동은 초기에는 선택 사항으로 두고, 기본 OG 미리보기가 정상 작동하는 것을 먼저 검증한다.
- 공유 문구에 예진이의 생일과 참여 목적이 드러나야 한다.

## 8. 데이터 모델 요구사항

구현 기술은 1.2장의 기준을 사용한다. 저장소가 이미 다른 기술로 구현되어 있으면 Codex는 기존 동작을 먼저 확인하고, 사용자 승인 없이 Prisma·데이터베이스·배포 방식을 교체하지 않는다. 논리적으로 다음 데이터를 보존해야 한다.

### 8.1 수신자/캠페인

```text
recipient
- id
- displayName: 예진
- birthDate: 2005-09-20
- birthdayLabel: 22번째 생일
- theme
- status
- createdAt
- updatedAt
```

최초 버전에서는 레코드가 하나지만, 이후 여러 사용자가 각자의 수신자를 만들 수 있도록 `recipientId` 또는 `campaignId`를 참여 기록에 포함한다.

### 8.2 참여 기록

```text
participation
- id
- recipientId
- displayName
- realName: nullable, 미래 확장용
- nameVisibility: display-only | private | omitted
- relationship: nullable
- relationshipCustom: nullable
- examAnswerId
- examScore: 0 | 100
- examTimedOut: boolean
- practiceAnswers: JSON 또는 정규화된 하위 레코드
- practiceCorrectCount: 0..3
- congratulationMessage
- clientSubmissionKey
- createdAt
```

서버 로그나 분석용으로 필요한 최소 정보만 저장하고, IP 주소·정밀 위치·불필요한 브라우저 식별 정보는 기본 저장하지 않는다.

### 8.3 콘텐츠 설정

스토리와 게임은 화면 컴포넌트에 하드코딩하지 말고 다음과 같은 콘텐츠 구조를 사용한다.

```text
content
- recipient
- storySlides[]
  - id
  - image
  - alt
  - text
  - actionLabel
- exam
  - introText
  - question
  - choices[]
  - correctChoiceId
  - timeLimitSeconds
  - successResult
  - failureResult
- practice
  - introText
  - rounds[]
    - id
    - prompt
    - choices[]
    - correctChoiceId
    - visualEffects
- finalText
- shareMetadata
```

### 8.4 Supabase 구현 기준

- Supabase 프로젝트는 사용 가능한 가장 가까운 아시아 리전에 둔다.
- `recipient`, `participation`, `content`에 해당하는 테이블 또는 동등한 구조를 Supabase PostgreSQL에 둔다.
- 모든 공개 스키마 테이블에 Row Level Security(RLS)를 활성화한다.
- 참여자는 로그인하지 않으므로 참여 기록 저장은 Next.js 서버 라우트 또는 Server Action을 통해서만 수행한다.
- `SUPABASE_SERVICE_ROLE_KEY` 또는 Supabase Secret Key는 서버 전용 환경 변수로만 사용하며 브라우저 번들에 포함하지 않는다.
- 예진이용 기록 조회는 Supabase Auth로 로그인한 소유자만 가능하게 한다.
- 콘텐츠와 정적 이미지는 최초 버전에서 코드와 `public/`에 보관하고, 참여 기록만 데이터베이스에 저장한다.
- 제출 중복 방지를 위해 `clientSubmissionKey`에 고유 제약을 둔다.
- 생일 행사 전에는 Supabase Table Editor 또는 CSV 내보내기로 기록을 백업한다.

### 8.5 환경 변수

실제 값은 저장소에 커밋하지 않고 `.env.local`과 Vercel 환경 변수에 등록한다.

```text
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SITE_URL=
```

`SUPABASE_SERVICE_ROLE_KEY`는 서버에서만 읽어야 하며, `NEXT_PUBLIC_` 접두사를 붙이지 않는다.

## 9. 오류·예외 처리

- 필수 입력이 비어 있으면 다음 단계로 이동하지 않는다.
- 서버 저장에 실패하면 사용자가 입력한 메시지를 잃지 않도록 재시도 버튼을 제공한다.
- 중복 제출은 기존 제출 결과를 반환하거나 “이미 저장된 기록입니다”를 표시한다.
- 이미지·폰트 로딩 실패 시 텍스트와 기본 플레이스홀더로 게임을 끝까지 진행할 수 있어야 한다.
- 시간이 끝난 시험은 결과를 확정하고, 뒤로 이동해 답을 바꿀 수 없게 한다.
- 기록 조회 실패 시 전체 오류 문구 대신 재시도 버튼과 연락 안내를 제공한다.
- 모바일 가로 회전 시 레이아웃이 깨지지 않게 하며, 필요하면 세로 사용 안내를 보여준다.

## 10. 접근성·성능·보안

### 접근성

- 이미지마다 의미 있는 대체 텍스트를 둔다.
- 색상만으로 정답·오답을 구분하지 않는다.
- 버튼은 키보드와 스크린 리더로도 접근 가능해야 한다.
- 텍스트 대비를 충분히 확보한다.
- 애니메이션을 줄이는 설정을 존중한다.

### 성능

- 모바일 네트워크에서 첫 화면이 빠르게 보여야 한다.
- 이미지에는 적절한 크기·포맷·지연 로딩을 적용한다.
- 게임 진행 중 큰 이미지가 갑자기 레이아웃을 밀어내지 않게 크기를 예약한다.
- 결과 기록 목록은 참여자가 많아졌을 때 페이지네이션 또는 무한 스크롤을 고려한다.

### 보안과 개인정보

- 이름과 메시지는 HTML이 아닌 안전한 텍스트로 렌더링한다.
- 서버에서 메시지 길이와 입력 형식을 다시 검증한다.
- 기록 저장 API에 기본적인 rate limit 또는 스팸 방지 장치를 둔다.
- 결과 페이지용 비밀값을 클라이언트 번들에 하드코딩하지 않는다.
- 참여자 입력으로 SQL, HTML, 스크립트가 실행되지 않아야 한다.
- 공개 공유 링크에는 예진이용 기록 조회 권한을 포함하지 않는다.

## 11. 분석 및 운영 요구사항

최초 버전에서는 개인 식별을 위한 추적을 하지 않는다. 운영에 꼭 필요한 수준에서 다음 이벤트만 고려한다.

- story_started
- participant_info_completed
- exam_completed
- practice_completed
- message_submitted
- share_clicked

분석 도구를 추가할 경우 사용자에게 불필요한 개인정보를 수집하지 않고, 분석 기능 없이도 핵심 사이트가 동작해야 한다.

## 12. 완료 조건

다음 항목을 모두 충족하면 최초 프로토타입의 핵심 기능이 완료된 것으로 본다.

- [ ] 스마트폰 세로 화면에서 오프닝 스토리 4장을 읽고 스와이프와 버튼으로 이동할 수 있다.
- [ ] 이름 필수 입력과 관계 선택/직접 입력이 동작한다.
- [ ] 시험 안내에서 5초 제한 퀴즈가 시작된다.
- [ ] 정답 2005년 9월 20일을 선택하면 100점 결과가 나온다.
- [ ] 오답·시간 초과 시 0점 결과와 생일 안내가 나온다.
- [ ] 시험 결과에서 실습으로 이동할 수 있다.
- [ ] 실습 3라운드가 순서대로 진행된다.
- [ ] 세 실습의 정답이 각각 간호사 옷, 슈거 사탕, 간호화로 설정되어 있다.
- [ ] 실습 정답 개수와 선택한 시각 요소가 최종 캐릭터에 반영된다.
- [ ] 축하 메시지를 입력하고 제출할 수 있다.
- [ ] 최종 화면에 이름, 시험 결과, 실습 결과, 메시지가 보인다.
- [ ] 제출 기록이 저장되고 중복 제출이 발생하지 않는다.
- [ ] 예진이용 기록 화면에서 참여자 이름과 메시지를 확인할 수 있다.
- [ ] 결과 화면은 일반 참여자에게 기본 공개되지 않는다.
- [ ] 카카오톡 등에서 제목·설명·대표 이미지가 포함된 링크 미리보기가 보인다.
- [ ] Vercel Hobby에서 정적 화면과 서버 저장 기능이 함께 동작한다.
- [ ] Supabase PostgreSQL에 참여 기록이 저장되고 RLS가 활성화되어 있다.
- [ ] 예진이용 결과 화면은 Supabase Auth로 보호되어 있다.
- [ ] 서버 전용 Supabase 비밀 키가 브라우저 번들에 노출되지 않는다.
- [ ] 생일 행사 전후에 Supabase 데이터를 CSV 또는 동등한 방식으로 내보낼 수 있다.
- [ ] 새로고침·뒤로 가기·네트워크 오류에 대한 최소한의 복구 처리가 있다.
- [ ] 키보드 접근, 대체 텍스트, 색상 외 정답 표시를 확인했다.
- [ ] 모바일 주요 기기 크기에서 레이아웃과 터치 영역을 검증했다.

## 13. 추후 확장안

### 13.1 여러 사용자를 위한 생일 축하 사이트 생성

추후에는 사이트 생성자가 다음 정보를 입력하면 하나의 캠페인을 만들 수 있게 한다.

- 수신자 이름과 생일
- 관계 또는 호칭
- 스토리 템플릿 선택
- 게임 템플릿 선택
- 대표 이미지와 색상 테마
- 결과 기록을 볼 수 있는 관리자 인증 정보

캠페인별로 콘텐츠와 기록을 분리하고, 생성자는 자신의 캠페인만 관리할 수 있어야 한다.

### 13.2 콘텐츠 편집기

- 스토리 슬라이드 추가·삭제·순서 변경
- 문구와 이미지 교체
- 시험 선택지와 정답 변경
- 실습 라운드 추가
- 최종 문구와 공유 메타데이터 수정
- 미리보기와 게시 전 검수

### 13.3 이름 맞히기 기능

참여자가 별명과 실명을 별도로 입력하고, 예진이에게는 별명만 먼저 보이게 한 뒤 예진이가 실명을 맞히는 기능을 추가할 수 있다. 이 기능은 실명 노출 동의와 비공개 저장 정책을 먼저 정한 뒤 구현한다.

### 13.4 장기적으로 좋은 생일 기능

- 생일 당일에만 열리는 마지막 편지
- 여러 메시지를 이어 붙인 타임라인
- 예진이가 하트를 누른 메시지 모음
- 참여자들이 남긴 메시지로 자동 생성되는 축하 영상/슬라이드쇼
- 만료일 이후 기록을 자동 삭제하는 개인정보 보관 정책
