# Codex 하네스: 예진이 생일 축하 사이트

문서 버전: 0.2.0  
적용 대상: 이 저장소에서 예진이 전용 생일 축하 사이트를 개발·검증·수정하는 Codex 작업  
기준 문서: `예진이_생일축하사이트_기능명세서.md`

## 1. 이 파일의 목적

이 문서는 Codex가 기능 명세서를 읽고 사이트를 개발할 때 지켜야 할 작업 방식, 모델 선택 기준, 의사결정 규칙, 검증 절차를 정의한다.

Codex는 기능 명세서와 이 하네스를 함께 읽은 뒤 현재 저장소의 구조·프레임워크·배포 환경을 조사한다. 저장소의 기존 규칙과 사용자가 이후에 전달하는 명시적 요구가 이 문서보다 우선한다.

## 2. 시작 시 반드시 수행할 일

작업을 시작할 때 다음 순서로 처리한다.

1. 기능 명세서와 이 하네스 전체를 읽는다.
2. 저장소의 `README`, `AGENTS.md`, `package.json`, 환경 변수 예시, 데이터베이스 설정, 배포 설정을 확인한다.
3. 현재 구현된 기능과 아직 없는 기능을 구분한다.
4. 사용자에게 보이는 동작에 영향을 줄 수 있는 모호한 부분을 결정 목록으로 작성한다.
5. 구현 전에 짧은 작업 계획과 변경 파일 목록을 제시한다.
6. 기존 코드를 함부로 재작성하지 않고, 현재 구조를 최대한 활용한다.

작업 중 명세서와 실제 저장소가 충돌하면 다음 우선순위를 사용한다.

1. 사용자의 최신 명시 요청
2. 보안·데이터 무결성·접근성에 관한 안전한 동작
3. 기능 명세서
4. 이 하네스의 구현 선호도
5. 기존 코드의 관습

충돌을 임의로 숨기지 말고, 작업 결과와 결정 로그에 남긴다.

## 2.1 고정 기술 스택과 무료 운영 기준

새로 시작하는 이 사이트의 기준 스택은 다음과 같다.

| 영역 | 기준 |
|---|---|
| 언어 | TypeScript strict mode |
| 프레임워크 | Next.js App Router + React |
| 스타일 | Tailwind CSS + 필요한 일반 CSS |
| 실행 환경 | Node.js LTS + npm |
| 배포 | Vercel Hobby, GitHub `main` 브랜치 자동 배포 |
| 데이터베이스 | Supabase Free PostgreSQL |
| 인증 | Supabase Auth, 결과 페이지 소유자 계정만 로그인 |
| 데이터 접근 | `@supabase/ssr`와 `@supabase/supabase-js` |
| 이미지 | 최초에는 저장소 `public/`, 추후 편집기에서 Supabase Storage |
| ORM | 신규 프로젝트에서는 사용하지 않음; Supabase SDK 직접 사용 |

이 사이트는 참가자가 로그인하지 않고 기록을 남겨야 하므로 정적 export가 아니라 서버 기능이 가능한 Next.js 배포를 사용한다. `output: 'export'`를 설정하지 않는다.

Vercel Hobby와 Supabase Free는 개인 프로토타입에 적합하지만 무료 한도와 정책은 변경될 수 있다. Supabase Free 프로젝트의 비활성 일시 중지와 백업 부재를 전제로 생일 행사 전후 데이터 내보내기 절차를 둔다. 실제 배포 직전에 공식 제한을 재확인한다.

### 2.1.1 Prisma 사용 예외

사용자가 이미 정상 동작하는 Prisma 기반 저장소를 제공하면 Codex는 Prisma를 자동 제거하거나 Supabase SDK로 마이그레이션하지 않는다. 기존 Prisma를 유지할지 전환할지는 다음을 확인한 뒤 사용자에게 제안한다.

- 현재 Prisma 버전과 연결 설정이 정상인지
- Vercel 서버리스에서 connection pooling이 안전한지
- 이미 존재하는 마이그레이션과 데이터가 있는지
- Supabase SDK로 전환했을 때 얻는 이점이 실제로 큰지

새 저장소를 만드는 경우에만 Prisma를 기본적으로 추가하지 않는다. 이 사이트의 최초 CRUD 범위에서는 Supabase SDK 직접 사용을 기본으로 한다.

### 2.1.2 권장 환경 변수

```text
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SITE_URL=
```

`SUPABASE_SERVICE_ROLE_KEY` 또는 Supabase Secret Key는 서버 전용이다. 클라이언트 컴포넌트, `NEXT_PUBLIC_` 변수, 로그, 오류 화면, 저장소 커밋에 절대 노출하지 않는다.

## 3. 모델 라우팅 정책

모델 이름은 현재 사용 가능한 GPT 모델 기준의 별칭이다. 특정 모델이 실행 환경에서 제공되지 않으면 가장 가까운 상위 안정 모델로 대체하고, 중요한 판단 결과는 동일한 검증 절차를 거친다.

### 3.1 기본 모델 역할

| 모델 | 기본 역할 | 사용해야 하는 작업 |
|---|---|---|
| Astra (`gpt-6-astra`) | 고난도 기획·설계·검토 | 제품 흐름 결정, 데이터 모델, 보안, 권한, 확장성, 큰 구조 변경, UX 의사결정, 최종 코드 리뷰 |
| Sol Light (`gpt-5.6-sol`, 낮은 추론 수준) | 반복적·범위가 분명한 구현 | 단순 컴포넌트 작성, 스타일 조정, 콘텐츠 설정 연결, 타입 추가, 작은 버그 수정, 포맷팅, 단위 테스트 보완 |
| Terra 또는 동급 코딩 모델 | 중간 난이도 구현·통합 | 여러 파일을 연결하는 기능 구현, API 연동, 상태 흐름 구현, 테스트와 코드 수정의 통합 |

### 3.2 Astra를 우선 사용하는 경우

다음 작업은 낮은 모델에 바로 맡기지 않는다.

- 전체 사용자 여정과 화면 상태 설계
- 시험 타이머·중복 제출·새로고침 복구와 같은 상태 무결성 설계
- 기록 조회 권한과 비공개 결과 페이지 설계
- 데이터베이스 스키마, 마이그레이션, 인증, rate limit
- 여러 사용자를 지원하기 위한 캠페인 구조
- 개인정보 보관·삭제 정책
- 새로운 라이브러리나 외부 서비스 도입 여부
- 접근성·모바일 UX의 최종 판단
- 큰 리팩터링 또는 기존 동작을 바꾸는 변경
- Sol Light가 두 번 연속 같은 문제를 해결하지 못한 경우

### 3.3 Sol Light에 맡겨도 되는 경우

다음 조건을 모두 만족하면 Sol Light를 사용한다.

- 요구사항과 완료 조건이 명확하다.
- 변경 범위가 한두 개의 독립 파일 또는 작은 기능 단위다.
- 데이터 구조·권한·보안 규칙을 새로 결정하지 않는다.
- 기존 패턴을 그대로 따르는 반복 작업이다.

예시:

- 스토리 카드의 문구와 이미지 설정 연결
- 버튼의 로딩 상태 추가
- 반복되는 실습 선택지 카드 컴포넌트 작성
- 정적 콘텐츠 타입 정의
- 이미 정해진 문구와 이미지 경로 교체
- 명확한 재현 절차가 있는 CSS 문제 수정
- 기존 테스트 패턴을 따르는 테스트 추가

### 3.4 모델 간 작업 인계

낮은 모델이 만든 변경도 자동으로 신뢰하지 않는다.

1. 작업을 맡길 때 입력·출력·수정 범위·검증 명령을 명확히 적는다.
2. 변경된 파일과 diff를 확인한다.
3. 타입 검사와 관련 테스트를 실행한다.
4. 화면 변경은 모바일 크기에서 직접 확인한다.
5. 보안·권한·데이터 저장에 닿으면 Astra 수준의 검토를 거친다.

### 3.5 기술 선택 작업의 모델 배정

- Astra: Vercel·Supabase·Next.js의 경계, RLS 정책, Auth 구조, 환경 변수, 서버/브라우저 클라이언트 분리, 무료 요금제 제한에 대한 판단
- Terra 또는 동급 코딩 모델: Supabase 클라이언트 초기화, 서버 라우트, Auth 미들웨어, SQL 마이그레이션 통합
- Sol Light: 이미 확정된 환경 변수 이름 연결, 콘텐츠 타입 추가, 반복 UI, 정적 이미지 경로 연결, 테스트 데이터 작성

무료 요금제의 제한을 근거로 외부 유료 서비스나 새로운 인프라를 추가하려면 Astra 검토와 사용자 확인이 필요하다.

## 4. 개발 원칙

### 4.1 콘텐츠와 화면 흐름 분리

스토리 문구, 이미지, 선택지, 정답, 성공·실패 문구는 컴포넌트 내부에 흩어 놓지 않는다. 한 곳의 콘텐츠 설정 또는 타입이 있는 데이터 파일에서 관리한다.

화면 컴포넌트는 다음을 담당한다.

- 현재 상태 표시
- 사용자 입력 수집
- 이동·검증·저장 이벤트
- 콘텐츠 설정을 화면에 렌더링

콘텐츠 설정은 다음을 담당한다.

- 스토리 슬라이드
- 시험 문제와 선택지
- 실습 라운드와 정답
- 결과 문구와 시각 요소 매핑
- 공유 메타데이터

문구를 바꾸기 위해 게임 로직을 수정해야 하는 구조를 만들지 않는다.

### 4.2 상태를 명시적으로 관리

최소한 다음 상태를 구분한다.

- `story`
- `participant-info`
- `exam-intro`
- `exam-playing`
- `exam-result`
- `practice-intro`
- `practice-playing`
- `practice-result`
- `message-input`
- `submitting`
- `completed`
- `error`

각 상태에서 허용되는 입력과 다음 상태를 명확히 한다. 시험 답안 확정, 실습 라운드 확정, 최종 제출은 한 번만 처리한다.

### 4.3 모바일 우선

- 320px 이상의 좁은 화면부터 설계한다.
- 주요 버튼은 충분한 터치 영역을 갖는다.
- 가로 스크롤이 생기지 않게 한다.
- 스와이프와 버튼을 함께 제공한다.
- 키보드가 올라와도 메시지 입력과 제출 버튼을 사용할 수 있어야 한다.
- 이미지 비율과 캐릭터 위치가 기기별로 크게 흔들리지 않게 한다.

### 4.4 감정과 안전의 균형

울고 있는 캐릭터는 귀엽고 가벼운 연출로 사용한다. 오답 사용자를 비난하거나 실패를 반복적으로 강조하지 않는다. 점수는 게임의 재미를 위한 결과이지 참여자 간 경쟁의 기준이 아니다.

### 4.5 과도한 범위 확장 금지

초기 프로토타입에서는 캠페인 생성기, 소셜 로그인, 복잡한 관리자 패널, 분석 대시보드, 실시간 채팅을 임의로 추가하지 않는다. 확장 가능하게 설계하되 현재 완료 조건을 먼저 만족한다.

### 4.6 Supabase와 Vercel 경계

- 참여자 브라우저에서 Supabase 데이터베이스에 직접 쓰기 권한을 주지 않는다.
- 참여 기록 저장은 Next.js Route Handler 또는 Server Action에서 입력을 검증한 뒤 수행한다.
- 서버 저장 클라이언트와 브라우저용 Supabase 클라이언트를 분리한다.
- 모든 공개 스키마 테이블에 RLS를 켜고, 마이그레이션에 grants와 policies를 함께 기록한다.
- 예진이용 기록 조회는 Supabase Auth 세션과 소유자 조건을 모두 확인한다.
- `service_role` 또는 Secret Key로 RLS를 우회하는 코드는 서버 전용 파일에서만 사용한다.
- 정적 이미지가 적은 최초 버전에서는 Supabase Storage를 사용하지 않고 `public/`에 둔다.
- Vercel에서 서버 기능을 사용해야 하므로 `output: 'export'`를 사용하지 않는다.
- Vercel Hobby의 실행 시간·요청·배포 제한을 넘는 장시간 작업, 대용량 업로드, 실시간 스트림을 추가하지 않는다.

## 5. 권장 구현 순서

아래 순서를 기본으로 사용하되 현재 저장소 상황에 맞게 조정한다.

### 단계 A: 기준선 확인

- 앱 실행과 빌드 명령 확인
- 현재 라우팅과 스타일 시스템 확인
- 저장소에 이미 있는 이미지·폰트·아이콘 확인
- 환경 변수와 데이터베이스 연결 상태 확인

### 단계 B: 콘텐츠 및 타입

- 예진이 수신자 정보
- 스토리 4장
- 시험 문제·선택지·정답·5초 제한
- 실습 3라운드·정답·시각 결과
- 최종 문구·공유 메타데이터

### 단계 C: 화면 흐름

- 스토리 화면
- 참여자 정보 입력
- 시험 안내·진행·결과
- 실습 안내·진행·결과
- 축하 메시지 입력
- 최종 결과

### 단계 D: 기록 저장

- Supabase PostgreSQL 마이그레이션
- Supabase RLS 정책과 grants
- 참여 기록 API 또는 서버 액션
- 서버·브라우저 Supabase 클라이언트 분리
- Supabase Auth 기반 결과 페이지 보호
- 입력 검증
- 중복 제출 방지
- 저장 실패 재시도
- 예진이용 보호된 기록 화면

### 단계 E: 공유와 품질

- OG 메타데이터
- 링크 복사/Web Share
- 접근성 점검
- 모바일 반응형 점검
- 성능과 이미지 로딩 점검

## 6. 기능 구현 세부 규칙

### 6.1 시험 타이머

- 타이머 시작 시점은 문제 화면이 실제로 표시된 시점으로 통일한다.
- 타이머는 화면 렌더링 횟수에 따라 여러 개가 생성되지 않아야 한다.
- 서버 시간으로 공정성을 맞출 필요가 없는 최초 버전에서는 클라이언트 타이머를 사용해도 되지만, 결과 확정은 한 번만 수행한다.
- 0초가 되면 시간 초과 오답을 확정하고 결과 화면으로 이동한다.
- 답을 선택한 뒤 타이머가 만료되어 결과가 덮어써지지 않게 한다.

### 6.2 실습 결과

실습 답안은 라운드 ID와 선택지 ID를 함께 저장한다. 선택한 시각 요소와 정답 여부를 별도로 계산할 수 있게 한다. 최종 화면은 고정된 문구만 보여주지 말고 실제 선택 결과에서 생성한다.

### 6.3 입력과 기록 저장

- 클라이언트와 서버 모두 길이·공백·허용 형식을 검증한다.
- 서버 저장 전 HTML을 제거하거나 텍스트로만 다룬다.
- `clientSubmissionKey` 또는 동등한 idempotency 방식으로 중복 제출을 막는다.
- 저장이 끝나기 전에는 완료 화면을 성공으로 표시하지 않는다.
- 네트워크 실패 때 사용자가 다시 입력하지 않도록 임시 데이터를 유지한다.

### 6.4 결과 페이지

- 참여자용 링크와 예진이용 링크를 분리한다.
- 기록 조회 권한은 서버에서 검사한다.
- 비밀번호·토큰·비밀 경로를 클라이언트 소스에 평문으로 노출하지 않는다.
- 결과 페이지에 참여자의 실명 확장 필드가 추가되더라도 표시 권한을 별도로 둘 수 있어야 한다.

### 6.5 환경 변수와 무료 서비스

- 로컬에서는 `.env.local`, 배포에서는 Vercel 환경 변수에 값을 등록한다.
- `.env.example`에는 키 이름만 두고 실제 값은 넣지 않는다.
- Supabase URL과 anon key는 공개 클라이언트에서 사용할 수 있지만, service role/secret key는 서버에서만 사용한다.
- Supabase Free의 비활성 일시 중지 가능성을 고려해 생일 행사 전날 실제 참여 흐름을 한 번 실행한다.
- 행사 후에는 Supabase 데이터를 CSV로 내보내거나 별도 안전한 장소에 백업한다.
- 무료 요금제의 현재 제한을 배포 전에 공식 문서에서 다시 확인하고 `DECISIONS.md`에 확인 날짜를 남긴다.

## 7. 테스트 전략

### 7.1 단위 테스트

다음 순수 로직은 UI와 분리해 테스트한다.

- 시험 정답 판정
- 시험 5초 시간 초과 판정
- 실습 각 라운드의 정답 판정
- 실습 정답 개수 계산
- 선택 결과에서 최종 캐릭터 요소 생성
- 입력값 검증
- 중복 제출 키 처리

### 7.2 통합 테스트

- 스토리 마지막에서 정보 입력으로 이동
- 정보 입력 후 시험 시작
- 시험 성공·실패·시간 초과 흐름
- 실습 3라운드와 결과 화면
- 메시지 제출과 완료 화면
- 저장 실패 후 재시도
- 예진이용 결과 조회 권한
- 비로그인 참여자의 기록 제출
- 비로그인 사용자의 결과 페이지 접근 차단
- 로그인한 소유자의 결과 조회
- 중복 `clientSubmissionKey` 제출 차단

### 7.3 브라우저·시각 검증

최소한 다음 화면 크기에서 확인한다.

- 360×800
- 390×844
- 412×915

확인 항목:

- 가로 스크롤 없음
- 버튼이 화면 밖으로 나가지 않음
- 캐릭터와 선택지가 겹치지 않음
- 타이머가 잘림 없이 보임
- 긴 이름과 긴 메시지가 레이아웃을 깨뜨리지 않음
- 로딩·오류·완료 상태가 보임
- 공유 메타데이터가 실제 배포 URL에서 읽힘

### 7.4 검증 명령

저장소에 이미 정해진 명령이 있으면 그것을 우선한다. 없다면 현재 프레임워크에 맞는 최소 명령을 확인해 사용한다.

```text
install dependencies
type check
lint
unit/integration tests
production build
```

Supabase를 사용하는 경우 다음도 가능한 범위에서 실행한다.

```text
supabase db lint
supabase test db
```

Supabase CLI를 사용하지 않는 환경이라면 같은 SQL 마이그레이션과 RLS 정책을 Supabase SQL Editor 또는 테스트 데이터로 검증하고, 실행하지 못한 명령을 보고한다.

명령을 실행하지 못했다면 성공했다고 말하지 말고, 실패 원인과 아직 확인하지 못한 범위를 명시한다.

## 8. 의사결정과 사용자 확인이 필요한 경우

다음은 임의로 결정하지 않고 사용자에게 확인하거나, 최소한 작업 전에 선택지를 제시한다.

- 예진이용 결과 페이지를 보호하는 구체적인 방법
- 실제 이미지·일러스트를 새로 만들지 기존 자산을 사용할지
- 데이터베이스와 호스팅 서비스 선택
- 무료 요금제 한도를 넘을 가능성이 있는 외부 서비스 추가
- 카카오톡 SDK처럼 외부 서비스 자격 증명이 필요한 기능
- 실명 필드를 최초 버전에 포함할지
- 메시지 보관 기간과 삭제 방법
- 공개 URL로 만들지 특정 사람만 접근 가능한 링크로 만들지

다음은 안전하고 되돌리기 쉬운 범위에서 Codex가 결정해도 된다.

- 이미 정해진 문구의 컴포넌트 배치
- 같은 기능을 구현하는 내부 함수명
- 디자인 토큰과 반응형 간격
- 플레이스홀더의 구체적인 모양
- 콘텐츠 설정 파일의 내부 구조

## 9. 작업 보고 형식

각 작업이 끝나면 다음 내용을 짧게 보고한다.

1. 구현한 기능
2. 변경한 파일
3. 사용한 모델 역할과 이유
4. 실행한 검증 명령과 결과
5. 아직 확인하지 못한 항목
6. 명세서와 다르게 결정한 사항
7. 다음 권장 작업

코드나 화면이 아직 완성되지 않았는데 “완료”라고 표현하지 않는다.

## 10. 완료 정의

기능 하나를 완료했다고 말하려면 다음을 모두 만족해야 한다.

- 기능 명세서의 해당 완료 조건을 충족한다.
- 정상 흐름과 최소 한 개의 실패 흐름을 검증했다.
- 모바일 화면에서 직접 확인했다.
- 타입·린트·관련 테스트를 통과했거나 실패 원인을 보고했다.
- 사용자 입력이 화면에 안전하게 렌더링된다.
- 기존 기능을 깨뜨리지 않았다.
- 변경 파일과 남은 위험을 보고했다.

전체 프로토타입의 완료는 기능 명세서 12장의 체크리스트와 이 하네스의 검증 절차를 모두 검토한 뒤 판단한다.

## 11. Codex 작업 시작 프롬프트

새 작업을 시작할 때 다음 지침을 기본 프롬프트로 사용한다.

```text
기능 명세서와 하네스 파일을 먼저 끝까지 읽어라.
현재 저장소의 구조와 실행 방법을 먼저 확인하라.
새 프로젝트라면 TypeScript + Next.js App Router + Tailwind CSS + Vercel Hobby + Supabase Free PostgreSQL 조합을 기본으로 사용하라.
새 프로젝트에서는 Prisma를 추가하지 말고 Supabase SDK를 사용하라. 단, 기존 Prisma가 정상 작동하는 저장소에서는 먼저 유지 가능성을 검토하라.
참여자는 로그인 없이 제출할 수 있어야 하며, 예진이용 결과 페이지는 Supabase Auth로 보호하라.
Supabase RLS를 모든 공개 테이블에 활성화하고 service role/secret key를 브라우저에 노출하지 마라.
Vercel 서버 기능이 필요하므로 output: 'export'를 사용하지 마라.
이번 작업의 목표와 완료 조건을 한 문단으로 요약하고, 변경 예정 파일을 제시하라.
기능 명세서에 없는 큰 기능을 임의로 추가하지 마라.
콘텐츠와 화면 로직을 분리하고, 모바일 화면과 접근성을 우선하라.
입력·타이머·제출·권한 관련 동작은 서버와 클라이언트 양쪽의 경계를 확인하라.
작업 후 타입 검사, 린트, 관련 테스트, 프로덕션 빌드를 가능한 범위에서 실행하라.
마지막 보고에는 구현 내용, 변경 파일, 검증 결과, 남은 위험을 포함하라.
``` 

## 12. 결정 로그 템플릿

중요한 선택을 할 때 아래 형식으로 저장소의 `DECISIONS.md` 또는 작업 보고에 남긴다.

```markdown
## [날짜] 결정 제목

- 상황:
- 선택지:
- 결정:
- 이유:
- 영향:
- 되돌리는 방법:
- 관련 명세 항목:
```

초기 프로토타입이 안정화되면 이 하네스와 기능 명세서의 버전을 함께 올린다. 기능 변경과 구현 변경을 섞지 말고, 사용자가 무엇이 바뀌었는지 이해할 수 있도록 변경 이력을 남긴다.
